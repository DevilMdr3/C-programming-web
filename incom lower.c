#include<stdio.h>
void conversion{char*};int main()
{
    char ch;
    printf("Enter character:");
    scanf("%d",&ch);
    conversion(&ch);
    printf("After conversion %c",ch);
    return 0;
}
void conversion[char*c]
{
    if(*c >=97 && *c <=122)
        *c=*c
}
